const axios = require('axios');
const model = require('./../../model');

module.exports = () => {
    router.post('/', async (req, res) => {
        //TODO(Taeyoung) : use signup function here
    });
};